enum TriggerType {
  THRESHOLD
  VARIANCE
}

enum Operator {
  GT // >
  LT // <
  GTE // >=
  LTE // <=
  EQ // =
}

enum NotificationChannel {
  IN_APP
  EMAIL
}

enum EventStatus {
  OPEN
  RESOLVED
}
